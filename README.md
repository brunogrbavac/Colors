# Colors
Simple random color generator ReactJS app using the @colr.org API.

## Setup

### `npm install`
Run before start to install needed dependencies.

### `npm test`
Run to start some basic tests.

### `npm start`
Run to start the Colors app.
